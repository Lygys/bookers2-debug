<div>
  <%= form_with(model:[book, ncomment], local: true) do |f| %>
    <%= render 'layouts/errors', obj: ncomment %>
    <div class="form-group">
      <%= f.text_area :comment, rows:'5', class: 'form-control　inputComment' %>
    </div>
    <div class="form-group">
      <%= f.submit "送信", class: 'btn btn-success' %>
    </div>
  <% end %>
</div>